{
    "name": "strava-cumulative-elevation-extension",
    "version": "1.0.0",
    "description": "",
    "main": "background.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "tsc && cp manifest.json dist/ && cp index.html dist/ && cp style.css dist/ && cp mountains.png dist/",
        "build-extension": "npm run build && cp manifest.json dist/"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@types/chrome": "^0.0.193"
    },
    "devDependencies": {
        "typescript": "^4.7.4"
    }
}
